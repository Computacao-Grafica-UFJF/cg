const gameConfig = {
    level: {
        width: 30,
        blocksDestroyedLimit: 1,
        viewBoundingBox: false,
        miniBall: {
            startSpeed: 0.15,
        },
    },
    session: {
        lives: 5,
    },
};

export default gameConfig;
