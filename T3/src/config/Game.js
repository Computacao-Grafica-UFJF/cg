const gameConfig = {
    level: {
        width: 30,
        blocksDestroyedLimit: 1,
        viewBoundingBox: false,
        miniBall: {
            startSpeed: 0.15,
            firePowerUpTime: 7000,
        },
    },
    session: {
        lives: 0,
    },
};

export default gameConfig;
