const gameConfig = {
    level: {
        width: 30,
        blocksDestroyedLimit: 10,
    },
    session: {
        lives: 5,
    },
    game: {
        miniBall: {
            startSpeed: 0.15,
        },
    },
};

export default gameConfig;
